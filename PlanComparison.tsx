import { plans } from "@/lib/site";
export function PlanComparison() { const rows=["credits","collections","uploads","processing","export","analytics","seats"] as const; return <section className="section section-light"><div className="section-shell"><p className="section-kicker">PLAN COMPARISON</p><h2 className="display">Compare the details.</h2><div className="table-wrap"><table><thead><tr><th>Feature</th>{plans.map(p=><th key={p.name}>{p.name}<small>{p.price}</small></th>)}</tr></thead><tbody>{rows.map(row=><tr key={row}><th>{row[0].toUpperCase()+row.slice(1)}</th>{plans.map(p=><td key={p.name}>{p[row]}</td>)}</tr>)}</tbody></table></div></div></section> }
