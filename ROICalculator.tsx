"use client";
import { useMemo, useState } from "react";
export function ROICalculator() { const [campaigns,setCampaigns]=useState(12); const rec=useMemo(()=>campaigns<=2?"Creator":campaigns<=8?"Pro":campaigns<=20?"Studio":"Enterprise",[campaigns]); return <section className="section section-paper" id="roi"><div className="section-shell roi-grid"><div><p className="section-kicker">CAPACITY ADVISOR</p><h2 className="display">How many campaigns<br/>do you create per month?</h2><p>Adjust the workload estimate to see the most appropriate starting plan.</p><label className="range-label"><span>{campaigns} campaigns</span><input type="range" min="1" max="40" value={campaigns} onChange={e=>setCampaigns(Number(e.target.value))}/></label></div><div className="recommendation"><small>RECOMMENDED STARTING POINT</small><h3>{rec}</h3><p>{rec==="Creator"?"For initial professional use and a lighter monthly content rhythm.":rec==="Pro"?"For professional creators producing consistently each month.":rec==="Studio"?"For high-volume creators and growing teams that need priority processing and deeper analytics.":"For organisations that need tailored capacity, integrations and commercial terms."}</p></div></div></section> }
